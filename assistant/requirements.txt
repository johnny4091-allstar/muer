# Muer Voice Assistant — Python dependencies
# Install with: pip install -r assistant/requirements.txt

# Audio I/O
pyaudio>=0.2.13

# Wake word detection
openwakeword>=0.6.0

# Speech-to-text (local Whisper)
faster-whisper>=0.10.0

# Text-to-speech
pyttsx3>=2.90

# Claude API (intent processing + Q&A)
anthropic>=0.28.0

# Numpy (required by openwakeword)
numpy>=1.24.0,<2.0.0

# HTTP requests (weather, news, sports, Wikipedia, smart home)
requests>=2.31.0

# Environment variable loading
python-dotenv>=1.0.0
